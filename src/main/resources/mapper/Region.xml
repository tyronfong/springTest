<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pri.tyron.test.mybatis.mapper.RegionMapper">
    <select id="selectRegion" resultType="pri.tyron.test.mybatis.beans.Region" parameterType="int">
        select * from region where r_regionkey = #{regionkey}
    </select>
</mapper>